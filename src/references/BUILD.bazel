load("@rules_rust//rust:defs.bzl", "rust_binary", "rust_test")

rust_binary(
    name = "references",
    srcs = ["exercise.rs"],
)

rust_test(
    name = "references_test",
    size = "small",
    crate = ":references",
)
