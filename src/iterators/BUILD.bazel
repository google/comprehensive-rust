load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "offset_differences",
    srcs = ["exercise.rs"],
)

rust_test(
    name = "offset_differences_test",
    size = "small",
    crate = ":offset_differences",
)
