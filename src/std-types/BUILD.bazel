load("@rules_rust//rust:defs.bzl", "rust_binary", "rust_test")

rust_binary(
    name = "hashset",
    srcs = ["exercise.rs"],
)

rust_test(
    name = "hashset_test",
    size = "small",
    crate = ":hashset",
)
